## 🚀 Quick Start

### Ubuntu (5 mins)
```bash
sudo apt install python3.10-venv git xclip jq tree
git clone https://github.com/GambitATJ/ai-os-agent.git
cd ai-os-agent
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

